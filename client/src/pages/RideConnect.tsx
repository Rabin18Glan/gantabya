import { PageWrapper } from '@/layouts'
import SectionWrapper from '@/layouts/wrappers/SectionWrapper'

function RideConnect() { 
  return (
    <PageWrapper>
 <SectionWrapper>
You are not connected
 </SectionWrapper>
  </PageWrapper>
  )
}

export default RideConnect